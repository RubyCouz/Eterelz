{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}

<div class="container" id="card">
    <div class="card">
    <h5 class="card-title">Votre profil</h5>
        <div id ="cardprofil" class="row no-gutters">
            <div id="show1" class="col-md-12">
                <img id="image" src="https://meetanentrepreneur.lu/wp-content/uploads/2019/08/profil-linkedin.jpg" class="card-img" alt="default_profil">
            </div>
            <div class="col-md-12">
                <div class="card-body">
                        <div id="rens" class="card-text">Login</div>
                        <div id="edit_rens">{{ eter_user.userLogin }}</div>
                        <br>

                        <div id="rens" class="card-text">Email</div>
                        <div id="edit_rens">{{ eter_user.userMail }}</div>
                        <br>

                        <div id="rens" class="card-text">ID Discord</div>
                        <div id="edit_rens">{{ eter_user.userDiscord }}</div>
                        <br>

                        <div id="rens" class="card-text">Adresse</div>
                        <div contentEditable=false data-text="Pas encore d'adresse..." id="edit_rens">{{ eter_user.userAddress }}</div>
                        <br>

                        <div id="rens" class="card-text">Code Postal</div>
                        <div contentEditable=false data-text="Pas encore de code postal..." id="edit_rens">{{ eter_user.userZip }}</div>
                        <br>

                        <div id="rens" class="card-text">Ville</div>
                        <div contentEditable=false data-text="Pas encore de ville..."id="edit_rens">{{ eter_user.userCity }}</div>
                        <br>

                        {#<div id="rens" class="card-text">Sexe</div>
                        <div id="edit_rens">{{ eter_user.userSex }}</div>
                        <br>#}

                        <div id="rens" class="card-text">Description</div>
                        <div contentEditable=false data-text="Pas encore de description..." id="edit_rens">{{ eter_user.userDescription }}</div>
                        <br>

                        <div id="rens" class="card-text">Rôle</div>
                        <div id="edit_rens">{{ eter_user.userRole }}</div>
                </div>
            </div>
        </div>
        <a type="button" id="modifier" class="btn btn-info" href="{{ path('eter_user_edit', {'id': eter_user.id}) }}">Modifier votre profil</a>
    </div>
    
</div>
{% endblock %}
