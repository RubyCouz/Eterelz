{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <h1>EterUser</h1>
<div class="container">
    <div class="card mb-3">
        <div id ="cardprofil" class="row no-gutters">
            <div class="col-md-4 pl-3">
                <img src="https://meetanentrepreneur.lu/wp-content/uploads/2019/08/profil-linkedin.jpg" class="card-img" alt="...">
            </div>
            <div class="col-md-8">
                <div class="card-body">
                    <h5 class="card-title">Votre profil</h5>
                        <p id="rens" class="card-text">Login : {{ eter_user.userLogin }}</p>
                        <p id="rens" class="card-text">Email : {{ eter_user.userMail }}</p>
                        <p id="rens" class="card-text">Adresse : {{ eter_user.userAddress }}</p>
                        <p id="rens" class="card-text">Code Postal : {{ eter_user.userZip }}</p>
                        <p id="rens" class="card-text">Ville : {{ eter_user.userCity }}</p>
                        <p id="rens" class="card-text">Discord : {{ eter_user.userDiscord }}</p>
                        <p id="rens" class="card-text">Genre : {{ eter_user.userSex }}</p>
                        <p id="rens" class="card-text">Description : {{ eter_user.userDescription }}</p>
                        <p id="rens" class="card-text">Rôle : {{ eter_user.userRole }}</p>
                </div>
            </div>
            <a type="button" class="btn btn-info m-auto" href="{{ path('eter_user_edit', {'id': eter_user.id}) }}">Modifier votre profil</a>
        </div>
    </div>

    {{ include('eter_user/_delete_form.html.twig') }}
</div>
{% endblock %}
